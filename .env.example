# OpenAI credentials (backend only — never commit real keys)
OPENAI_API_KEY=your-openai-api-key-here

# Agent tool orchestration model
OPENAI_AGENT_MODEL=gpt-5.6-terra

# Semantic safety checker model
OPENAI_MODEL=gpt-5.6-luna
